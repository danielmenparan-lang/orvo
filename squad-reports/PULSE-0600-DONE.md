# Pulse ~06:00 UTC — share links, ratings, Netlify

## Shipped
1. Hide cancelled requests by default + toggle; Copy link (`?rid=`)
2. Builder star average on client quote cards
3. `netlify.toml` + `/hire` `/builders` redirects
4. `sql/012_notifications.sql`; webhook uses `_shared/cors`
