# Pulse ~07:00 UTC — notif realtime + status notify

## Shipped
1. Notifications Realtime INSERT → toast + badge
2. `sql/015_status_notify.sql` — status changes → inbox
3. Post/quote char counters; admin Copy events (persisted buffer)
4. Checkout Edge body/JWT validation; badge pop motion
